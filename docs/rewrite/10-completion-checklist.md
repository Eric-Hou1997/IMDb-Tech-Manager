# 第 9–12 步逐项实施与验收

本清单重新按维护者指令建立。每行必须有对应实现和证据才可勾选；构建、安装烟测、静态测试不替代完整业务和桌面验收。此前 18 个包是验证包。

## 第 9 步：TCM Emby 集成和生命周期

- [ ] 只读 NFO → 索引 → 卡片数据 → 资源发布 → Emby 加载 → 实际卡片展示 → 服务停止与清理完整通过。
- [ ] Emby 环境发现、版本检查、安装目录与数据目录配置、路径映射。
- [ ] 安装、更新、修复、禁用、移除与重复操作。
- [ ] 历史组件识别、可审查维护计划、明确确认、权限申请与失败恢复。
- [ ] 外部备份、事务提交、并发修改保护、中断回滚。
- [ ] Web Card 数据与八种语言发布；Manager 与卡片发布状态分别显示。
- [ ] 服务启停、后台运行、租约失效、异常退出和正常退出清理。
- [ ] Emby 升级后自动重新检查与可操作修复。
- [ ] macOS/Linux 独立路径与权限适配。
- [ ] Windows ARM64 Manager 与真实 Emby 安装组合验证。

## 第 10 步：完整界面与系统行为

- [ ] 两产品每个原版页面、按钮、菜单、设置、快捷入口都有对应实现。
- [ ] 表格排序、搜索、筛选、选中项、布局状态持久化。
- [ ] Movie/TV 独立状态、TV 层级、季选择、批量范围准确。
- [ ] 长任务真实进度；错误可回到受影响项目。
- [ ] 最小化、关闭窗口、后台运行、完全退出、二次启动与登录启动。
- [ ] 原生菜单、托盘、对话框、权限和错误提示语言一致。
- [ ] 三种内置语言与五种外部语言包；切换不丢失界面或任务状态。
- [ ] 八种语言、窄窗口、缩放、键盘与无障碍操作验收。
- [ ] Linux 缺少托盘等能力时有等效入口，并记录真实平台差异。

## 第 11 步：数据迁移与 OTA

- [ ] 旧配置、媒体库、路径映射、提示词、凭据、索引、缓存、备份、任务历史和启动设置逐字段迁移。
- [ ] 迁移可重复执行、中断恢复、回退；旧数据保留且不可静默丢弃。
- [ ] 产品、系统、应用架构、安装渠道精确选择更新产物。
- [ ] macOS ARM64 DMG 初装和应用内更新。
- [ ] Windows x64/ARM64 一次点击下载、验签、安装、重启及结果验证。
- [ ] Linux x64/ARM64 AppImage 应用内更新。
- [ ] Linux x64/ARM64 DEB/RPM 签名软件源及包管理器升级方案。
- [ ] Windows ARM 机器上的旧 x64 安装保持应用架构，避免重复安装和数据目录变化。
- [ ] 应用替换失败与数据迁移失败分别恢复；没有无法判断的半安装状态。
- [ ] 旧 OTA 签名与新格式兼容验证；既有密钥不自动更换。

## 第 12 步：最终验收与本地交付

- [ ] 功能表：原版功能全有实现与验证，无未解释遗漏；补齐第 5–8 步尚未接通的 ITM 获取、写入、AI 和审批链。
- [ ] 缺陷表：已知新增缺陷清零，每项修复有回归证据。
- [ ] 平台表：10 个应用目标、18 个安装包均通过实际安装、升级、卸载和桌面验收。
- [ ] 迁移表：旧数据、更新、失败恢复、ITM→TCM 衔接通过。
- [ ] 扫描、批处理、缓存、后台常驻长时间稳定性。
- [ ] 安装、升级、卸载、二次启动、异常退出与恢复。
- [ ] 新旧 ITM/TCM 数据兼容组合验证。
- [ ] 检查实际运行产物；完成替代后退出旧 Go/Python/PowerShell 业务引擎。
- [ ] 八种语言 README、隐私政策、条款、安装更新说明完整同步。
- [ ] 对最终交付提交重新验证并生成本地安装包、更新产物、签名、校验清单和语言包。
- [ ] 所有验收通过后才处理旧实现与主线替换；当前只同步重写分支，不推 main、不建新 tag、不发布 GitHub Release。

## 当前实施记录

TCM 新增 Rust `emby` / `card_service` 模块及 Tauri 维护界面，包含公开 schema 7 索引、计划确认、备份、事务回滚、安装/修复/移除和服务租约。卡片渲染资源从 v4.1.0 基线独立保留；不调用旧 PowerShell 引擎。新增 9 项回归及原有 14 项读取回归在本机通过，桌面编译和前端构建通过。真实 Emby 页面云端验收正在接入；本记录不勾选整项完成。

### 本轮增量证据

- 两产品新增旧数据快照计划、SQLite schema 2 原子导入与桌面确认入口。每产品 5 项迁移回归通过：原始日志/自定义提示词保留、重复请求、最后一个文件改变时整体回滚、操作 ID 冲突、凭据边界、离线/禁用根及畸形 JSON 保留。导入原字节和合并可用目录不等于所有旧业务适配完成，回执明确记录待核对状态。
- TCM 在真实 Emby 4.9.5.0 的 Linux x64/ARM64 服务器上通过 Rust 读取/发布 → 实际客户端展示 → 停止隐藏 → 重启显示 → 移除恢复链；NFO 字节和纳秒级 mtime 保持。云端运行 34564875724，源提交 49db938。Windows/macOS 组合继续验收。

### 真实 Emby 五目标增量

TCM 源提交 a435dcc：Linux x64/ARM64 运行 34567673874，Windows x64/ARM64 与 macOS ARM64 运行 34567673854 全部通过。实际链路使用官方 Emby 4.9.5.0、真实 NFO/视频、正式 Rust Store/Integration/CardService 及官方 Web 客户端，验证显示、停止隐藏、恢复、移除、服务重启及清理。Windows ARM64 是原生 Rust 管理核心加 x64 Emby 仿真组合；Windows x64 节点为 Server 2022。此证据不替代 Tauri 管理界面的完整交互和所有发行版验收。

新增实现正在继续验收：Emby 发现、安装/数据目录持久化、服务器版本检查、旧卡片显式迁移、八种卡片语言和语言切换。未完成的整项仍不勾选。

### OTA 增量与明确边界

ITM 已用原有 Ed25519 私钥验证 Tauri Minisign 格式签名；原密钥和公钥不变，篡改产物拒绝。新增精确目标选择、下载/校验进度、数据库快照、macOS 原子目录交换、AppImage 原子替换、旧应用保留和恢复。新增正常写入冻结验证。TCM 的签名信任根仍待维护者确定，更新插件未启用；不得复用 ITM 密钥。真实跨版本升级、完整数据失败恢复、软件源及旧客户端桥接尚未验收，因此第 11 步保持未完成。

### 界面与生命周期增量

Movie/TV 独立的搜索、筛选、排序、根目录范围、选择、展开和分页写入 Rust 权威状态；保存使用操作 ID 和版本冲突检查。TV 按真实目录归属构建节目/季/集，季选择覆盖未显示的已索引剧集；歧义归属保留为待定位条目。窗口关闭策略、后台恢复、退出清理和登录启动具有持久化设置与中断恢复。Linux 无可靠托盘时保留任务栏最小化入口。

本机实际 ITM Tauri 窗口验证：Movie 中文搜索“皇家”得到 4 项并选中 1 项，切到 TV 后状态独立；展开节目后选择第 1 季得到 10 项；返回 Movie 保持原搜索与选择；完全退出后进程不存在；重新启动仍恢复 TV 展开和 10 项选择。此记录只覆盖这些操作，不能替代全量桌面、所有语言、系统登录及更新验收。

### 混合媒体根目录与 Linux 登录回归（2026-09-11）

- 同一物理目录允许配置独立的 Movie 和 TV 根，按 NFO 类型分流；未知/损坏 NFO 在两个范围各自保留可定位异常，单一范围刷新不会删除另一范围索引。旧 mixed 配置迁移为两个独立范围，不移动媒体文件。
- 本地 reading 15 项和 migration 7 项通过。
- 云端 35b7e82 修复了 Linux 并行子进程继承数据库锁导致的重启失败；GIO 的百分号可执行路径仍失败。已定位到 GLib 在字段展开前检查 argv[0]，登录入口改为系统 /usr/bin/env 直接 exec 应用参数，仍需下一轮真实 GIO 验证。
- 本记录只覆盖上述行为，不表示第 9–12 步整体通过。

### ITM Inspector 规格编辑的实际链路（2026-09-11）

- 规格编辑已接通原生 Inspector、逐文件差异预览、确认写入、候选哈希绑定、持久化操作 ID、源文件 CAS、校验备份、索引刷新、历史与撤销。无变化不写文件；外部后续修改阻止撤销；退出/更新与写入所有者协调。
- Windows 使用 ReplaceFileW 保留安全描述符及附加流，保留独立校验备份和同目录原生恢复副本；文件内容逐个 flush。Windows 不具有与 Unix 目录 fsync 等价的当前适配，断电级持久性不能从进程中断测试推断。Windows 原生写入/撤销、ACL 和附加流回归待本提交云端结果。
- 本地完整核心回归通过；新增写入业务流程 5 项及事务/规格 11 项通过，严格 clippy 与前端构建通过。
- macOS 原生窗口实际操作了 Casino Royale 原始样本的隔离副本：预览不改文件，确认后 Camera 进入文件及 Inspector，Technical Specs 外字节和权限不变；从历史预览并确认撤销后整文件 SHA256 与原始样本完全相同。原始样本未修改。
- 该实现目前覆盖手动规格编辑与撤销，不代表 IMDb 获取、标签写入、AI、批处理、sidecar 同步及第 9–12 步全部完成。
- 上一提交 3afbefa 的五个构建/安装运行目标全部通过（34578464559），包括两个 Linux GIO 特殊路径登录回归。

Windows 写入回归发现候选 NamedTempFile 仍持有打开句柄，导致 ReplaceFileW 以无共享方式打开候选时返回 sharing violation（运行 34582170365，两个 Windows 目标）。已将候选转为只拥有清理路径的 TempPath，先关闭句柄再替换；原有真实写入、撤销、崩溃恢复及 ACL/附加流测试继续作为回归，等待新云端结果。本地 16 项写入测试通过。

### Windows 权限恢复与 IMDb 获取实际链路（2026-09-11）

- `90c2640` 的五目标构建/安装运行回归全部通过（34586486070）。候选句柄关闭后，进一步发现 ReplaceFileW 的 ACL 合并会累积授权条目；现保存原始 DACL 并恢复继承策略，元数据恢复具有独立中断状态，不能仅凭内容哈希判断成功。Windows x64/ARM64 的真实写入、撤销、ACL 和附加流回归通过。
- IMDb 获取已接通 Rust 请求记录、缓存、强制刷新、取消、重启中断状态和 Inspector 获取历史。普通 HTTP 失败后可使用隔离系统 WebView；不调用外部浏览器，不向远程窗口授权管理命令。原生读取回调取得结构化数据后验证标题 ID、JSON/schema、尺寸和有效规格；请求的每个传输尝试保留失败分类。
- 首次插入、来源刷新、保留手动有效规格、更新来源快照和派生标签过期状态进入同一逐文件预览/事务/撤销链。预览可查看完整原节点与候选节点。首次插入不改变根标签、BOM、换行或其他节点；TV 的 mediatype 继续使用原有 tvshow 协议值。
- macOS ARM64 原生窗口取得真实 tt0061452 数据：HTTP WAF → 系统 WebView → 差异预览 → 确认写入隔离副本 → Inspector 成功反馈 → 历史撤销。Technical Specs 外字节及权限不变；撤销后整文件 SHA256 恢复。另验证缓存命中、进行中的刷新取消、获取历史重启恢复及完全退出后无应用进程。详见 [本机证据](evidence/native-imdb-refresh.json)。
- 新增核心获取流程 4 项、来源构造/解析 7 项回归通过，覆盖首次插入撤销、缓存/强制刷新/取消、重复操作、重启、外部修改冲突、手动来源快照、畸形字段和 TV 元数据。真实 Windows/Linux IMDb 获取、HTML 结构变化回退、全库自动任务、旧缓存适配、sidecar、标签与 AI 仍需继续实现和验收；此增量不关闭整体功能门禁。

### 标签写入与 Inspector 归属操作增量（2026-09-11）

- `4bf74d6` 的五目标构建/安装运行全部通过（34592065448）。
- 原版 `edit_nfo` 的标签编辑、删除、显式归属调整、新增 Manual、清除 AI 标签已接入 Rust 候选构造与原生 Inspector 预览。Generated 手动编辑转为 Manual，External 编辑不接管归属，External 删除需显式勾选确认后再预览、确认写入。自动规则候选只替换权威 Generated，匹配现有 External/Manual 的值不插入、不认领。
- Writer 的写入意图持久化并与候选、操作 ID 绑定；标签候选由授权计划重新构造验证。撤销只接受对应原操作的精确校验备份，不放宽成任意 XML 替换。原有 Specs-only 写入边界保持。
- NFO 事务完成后才从嵌入 manifest 更新 sidecar。旁文件失败记录 `committed-mirror-pending`，重启恢复只补写旁文件，不重复替换 NFO。标签/归属、外部冲突、未知 schema、重复归属、崩溃恢复、旁文件失败恢复及撤销共 7 项新增行为回归通过；既有获取和写入用例继续通过。
- 本机原生窗口验证 Casino Royale 隔离副本新增中文 Manual 标签、预览不写、确认后 Inspector 与文件一致、历史撤销恢复整文件哈希。详见 [本机标签证据](evidence/native-tag-edit.json)。规则批处理、AI 请求与审批、旧 sidecar 迁移及各目标完整桌面验收继续进行，不将此增量计为第 8 或 12 步整体完成。

### ITM AI 请求、候选审批与恢复增量（2026-09-11）

- `f710e9a` 的五目标构建、安装运行及标签回归全部通过（34595152166）。本增量把原版 OpenAI/Anthropic 请求、中文默认提示词、Qwen 非思考与缓存策略接到 Rust 原生 HTTP、系统凭据存储、SQLite 请求历史及 Inspector。前端无法指定任意凭据账号；保存失败只回收本次新增且明确未提交的凭据，未知结果保留操作 ID 查询恢复。
- 每次真实请求在发出前记录，收到 usage 后先记账再校验结果。截断只提高实际输出上限后重试，JSON/schema 使用不同恢复请求；鉴权、额度、限流、传输及提供商错误分别保留。未改变的失败默认跳过，显式重试可以再发；鉴权/额度暂停后续 AI。显式配置的规则回退保留 AI 失败和费用，不能掩盖账户错误。
- AI 结果先形成候选与差异，再通过既有权威归属、CAS、备份、sidecar 与撤销链写入。缓存命中本次零请求、零费用，历史 usage/费用单列；中断请求不自动重发，无法取得的 usage 明确未知。
- 本机原生窗口对隔离媒体副本完成：截断后增大 2000→10000、两次 usage 合计、预演不写、审批写入、External 与事实层保持、sidecar 一致、撤销恢复完整哈希、缓存不发请求、取消、规则写入撤销、重启恢复历史及退出清理。HTTP 服务为本地测试替身，没有向付费模型发送请求；详见 [原生 AI 证据](evidence/native-ai-generation.json)。
- 新增 AI 流程 7 项回归，包括凭据/配置提交失败、未知 HTTP 结果、取消计费、缓存、明确重试、预算与规则回退；原生 HTTP 测试覆盖两协议请求、拒绝重定向和 Retry-After。批量范围、任务暂停恢复、旧 AI 数据适配、八语言界面及真实跨平台桌面链仍继续迁移，本记录不关闭第 8 或 12 步。

### 批量任务与确认空规格增量（2026-09-11）

- AI 源提交 `513e13d` 的五目标构建、安装运行与核心/原生 HTTP 回归全部通过（34600902659）。
- 对照旧 `tag_generate`、selected preview/approve 和 batch_control_contract，批量规则、AI 与 IMDb 获取接入现有 Rust 任务中心。默认使用已选条目；完整根目录范围必须明确选择，再展示每个 NFO、数量、媒体空间与任务语言后确认。只预演最多 10 项、写入计划最多 10000 项，沿用旧入口限制。普通生成跳过已完成状态；重建、失败项重试和预演分别记录意图。
- 每个文件固定请求/写入 ID 与原始哈希；完成当前文件后才响应暂停/取消，不把请求暂停显示为已经停止。中断恢复复用原 ID，已提交事务不再次写入；未知 HTTP 结果不自动重发。需要审批的 AI 结果保留原候选，批准后不重新调用模型。任务快照保留配置与语言，预算按批次实际请求/usage/费用累计。
- SQLite schema 7 将逐文件明细移到独立表；进度和任务列表只传摘要，明细按需加载、每页最多显示 100 项。每项检查点只更新该行，不重写整批明细；早期重写分支的内嵌任务格式原子迁移，旧版本拒绝打开新 schema。新增 6 项批量回归覆盖确认、范围、暂停/恢复/取消、审批、外部冲突、写入后中断、跨文件预算和明细迁移。
- 对照旧 `process_nfo` 还补齐 IMDb 已确认无规格的完成状态：已识别结构且明确为空才记录 empty、缓存并允许预演写入；缺失或未知结构仍失败。生成器不会从空规格自动删除外部标签。新增空结果缓存、重启、写入撤销与未确认空载荷保护回归通过。
- 自动运行调度、全部旧缓存/任务/凭据适配、完整八语言界面及最终跨平台业务验收继续实施，本增量不代表第 8–12 步整体完成。

本机原生窗口完成隔离副本的批量规则生成、明确全根目录范围的只预演、保存差异的逐项审批、Inspector 定位和两次撤销；最终整文件哈希恢复。重启后旧批量明细迁移为 schema 7 独立行，两个任务与逐项记录仍可打开。见 [原生批量证据](evidence/native-batch-generation.json)。本机只验证规则批量 UI，AI/IMDb 批量的真实网络桌面组合继续验收。

### 旧 AI 配置与系统凭据适配增量（2026-09-11）

- 旧 `config.json` 的 `ai` 节点在迁移确认页展示为可核对的配置方案；按旧 Go 加载规则识别 OpenAI/Anthropic 协议、旧默认值修正与额外请求 JSON，保留自定义提示词原始空白、模型、采样、思考/缓存、重试、审核/回退、单价与预算。配置和原始文件快照在同一 SQLite 事务生效；预览后目的配置发生变化则整体回滚，不覆盖新选择。
- macOS 导入配置引用旧 `local.imdb-tech-manager.ai` / `api-key` 系统凭据；原条目不复制到数据库、不删除、不覆盖。其他系统不假定能读取 macOS Keychain，保留配置并要求在 AI 设置重新配置系统凭据。未知协议或无效参数保留原始文件并列为需要复核，不擅自改成另一协议。旧 `inferred` 清理配置仍遵守新版权威归属边界。
- 普通 JSON 与字符串编码的 `extra_body` 中的凭据字段均阻止进入通用历史快照。新增 4 项迁移回归通过，包括提示词/配置保留、重启、重复执行、目的配置冲突、未知协议和嵌入凭据。
- 本轮同时补齐重写 AI 的启用开关：关闭时单文件和批量入口均拒绝新请求，历史仍可查看，界面提供明确启用项。对应回归通过；云端和原生系统凭据许可验收仍独立记录。

原生 macOS 迁移界面使用无凭据隔离配置验证：确认前未应用，确认后模型/协议/重试/预算和自定义提示词一致，原文件与归档字节相同；测试保持 AI 禁用，没有读取旧 Keychain 密钥或发送模型请求。退出后仅移除该测试配置，见 [原生迁移证据](evidence/native-ai-migration.json)。

### 自动运行与原生补全链路（2026-09-11）

- 对照原 Go `agentLoop`、启动偏好迁移及 Python `run("auto")`：30–86400 秒轮间隔、Movie/TV 独立任务、90–900 秒近期窗口优先、每轮一个存量补齐、仅 Technical Specs、不生成标签。近期路径与存量路径保留原版不同的失败冷却行为；原版隔离行为测试实际运行后得到相同的近期两项加存量一项写入范围。
- 空索引先建立只读索引，已有索引按摘要选择；不在每轮递归扫描 NAS。配置根是唯一范围来源；移除的根不会经 TMM 自动发现而重新加入。解析器修订更新后旧摘要先重建，mtime 与规格就绪状态进入索引。
- 自动任务与手动任务共用唯一工作线程、写入事务和历史。手动工作优先，自动补全在当前文件完成后让位；停止不更改下次启动偏好，启动偏好不更改系统登录项。应用重启按保存的明确选择启动，自动写入继续使用来源校验、固定操作 ID、备份和撤销。
- 旧 Manager `settings.json` 的间隔与新/旧自动启动标志支持预览和事务导入；新标志优先，旧 alias 只在已配置时采用。导入不启动后台或修改系统登录项，运行中不能导入覆盖自动设置。
- 新增 8 项 Rust 回归覆盖候选、间隔、手动抢占、停止、重启、只读冷索引、实际 Specs 安全写入/撤销、失败冷却与设置迁移。Rust 核心全部测试和原生 HTTP 回归、严格 clippy、前端构建通过。
- 独立原生 Tauri 应用在本机实际完成空索引 → 自动任务 → IMDb HTTP 202 → 隔离系统 WebView 成功 → 写入 → UI 完成 → 停止 → 启动偏好恢复且不重复请求 → 历史撤销 → 完全退出。原始 NFO 完整哈希恢复，External、未知 XML、BOM 与 CRLF 保持；正式样本未修改。见 [原生自动补全证据](evidence/native-automatic.json)。
- 上一配置迁移提交 `0d79c74` 的五目标全部通过（34607437710）；macOS 首次在 DMG 工具阶段失败，重跑同一源码后打包、安装与启动通过。后续构建增加详细日志，以便再次失败时定位原因。当前自动模式增量仍需五目标云端验证；旧缓存/历史/备份全面适配、八语言产品界面、OTA 和最终交付继续实施。
