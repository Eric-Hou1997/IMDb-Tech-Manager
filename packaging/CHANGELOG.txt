IMDb Tech Manager macOS v4.1.0

- 简体中文、繁體中文与 English (United States) 继续内置；新增法语、俄语、日语、西班牙语和泰语可下载语言包。语言包随本版本的 GitHub Release 发布，由 v4.1.0 内置目录唯一绑定版本与修订，不需要单独执行“更新语言包”。
- 语言选择器使用固定矩形国旗图标、当前界面语言名称和语言自身名称；未安装语言显示通用下载图标。语言包经过大小、文件名、ZIP 结构、清单、消息集合与 SHA-256 校验后原子安装到用户数据目录。
- Web、Go Core、Python Engine 与 macOS 原生菜单共用语言状态。任务在启动时固定日志与复核语言；旧任务、缓存、NFO、Ownership、默认系统提示词及用户自定义提示词均不改写。
- 更新检查增加条件缓存、退避与代理兼容处理，并分别呈现网络/代理连接失败、GitHub 主限额、二级限流、无明确限额的 403、发布资产缺失、下载失败和签名验证失败。
- 电视剧树的展开符号、状态灯、复选框和标题按同一层级缩进；季节点可一次选择当前季全部单集。AI/规则状态徽章与标签页样式统一。
- 发布语言包新增完整覆盖、占位符与协议标记、目标文字及错位译文门禁；Technical Specs、Tag、JSON 和 Ownership 的结构化协议不随语言变化。

IMDb Tech Manager macOS v4.1.0

- Simplified Chinese, Traditional Chinese, and English (United States) remain built in. French, Russian, Japanese, Spanish, and Thai are downloadable language packs attached to this release. The v4.1.0 catalog binds each pack revision to this app version, so there is no separate language-pack update operation.
- The language picker uses fixed rectangular flag icons, the current UI-language name, and each language's native name; uninstalled languages show a universal download icon. Packs are size, filename, ZIP-layout, manifest, message-set, and SHA-256 checked before atomic installation into user data.
- The Web UI, Go Core, Python Engine, and native macOS menus share language state. Tasks capture their log and review language at start; historical tasks, caches, NFO files, Ownership, the default system prompt, and custom prompts are not rewritten.
- Update checks now use conditional caching, backoff, and proxy-aware handling, while separately reporting network/proxy connection failures, GitHub primary limits, secondary throttling, unexplained 403 responses, missing release assets, download failures, and signature failures.
- TV expanders, status dots, checkboxes, and titles now share hierarchy indentation; a season checkbox selects every episode in that season. AI/rules badges use the same visual weight as tag badges.
- Release packs have full-coverage, placeholder/protocol-token, target-script, and translation-alignment gates. Structured Technical Specs, Tag, JSON, and Ownership protocols remain language-independent.

--- Published v4.0.4 / 已发布 v4.0.4 ---

IMDb Tech Manager macOS v4.0.4

- 新增 Web、Go Core、Python Engine 与 macOS 原生界面的简体中文 / English (United States) 双语支持；切换界面语言时不重载应用。
- 新任务在启动时固定日志与复核说明语言，运行中切换界面不会产生混合日志；旧任务日志保留原文并标明历史语言。
- 结构化状态、Tag、Technical Specs、JSON 协议和 Ownership 不随界面语言变化；默认系统提示词与用户自定义提示词均不改写。
- macOS 菜单、系统确认按钮、启动失败提示和文件访问权限说明跟随所选语言；英文 About 链接指向英文隐私政策与使用条款。
- 响应式布局依据实际内容宽度决定换行，为后续语言扩展保留与语言无关的测量机制。

IMDb Tech Manager macOS v4.0.4

- Adds Simplified Chinese and English (United States) across the Web UI, Go Core, Python Engine, and native macOS interface without reloading the app when switching languages.
- A task fixes its log and review language when it starts, so changing the interface mid-run cannot create mixed-language logs. Historical logs remain unchanged and are labeled with their original language.
- Structured statuses, Tags, Technical Specs, JSON protocols, and Ownership do not change with the interface language. The default system prompt and every custom prompt remain unmodified.
- Native macOS menus, system confirmation buttons, startup failures, and file-access permission descriptions follow the selected language. English About links open the English privacy policy and terms.
- Responsive wrapping is based on measured content width and remains language-independent for future locales.

--- Published v4.0.2 / 已发布 v4.0.2 ---

IMDb Tech Manager macOS v4.0.2

- “关于”区域显示真实发布日期 2026-09-02，不再包含发布占位文字。
- 每次打开设置页面都会自动检查一次 GitHub 正式更新，并在原“软件更新”区域显示检查中、最新版本、已有新版或失败状态。
- 更新检查增加并发保护，重复打开或点击不会同时发起多次请求。

- 列表与预览分栏、表格列顺序/宽度、任务中心展开状态和 CLI 高度可在重启后恢复；窗口尺寸与位置由原生窗口层验证并恢复。
- 统一电影与电视剧列表的响应式工具栏、状态筛选器比例、表头高度与列布局，避免切换页面或改变宽度时出现不一致的换行和遮挡。
- 预览导航与列表表头对齐，当前预览标签使用黄色短横线标识。
- 按钮、下拉选择器和结构性标题禁止文本选中；说明、状态与内容文本仍可复制。
- “启动后自动开启后台模式”与“登录后启动应用”解耦，旧设置安全迁移为新的启动偏好。
- 新增 IMDb 缓存上限设置（默认 2048 MB，范围 64–65536 MB）、用量状态、清理操作和 LRU 维护；缓存清理避开符号链接、未知文件与正在使用的条目。
- IMDb 抓取缓存验证原始内容完整性，并将 WAF/限流等抓取失败保留为明确状态，避免误判为无技术规格。
- 批量审批任务以不可变任务 ID 等待和回刷，重叠运行请求明确失败，避免回刷到错误 NFO。
- 本地规则生成与预演记录统一使用 4.0.0 规则标识；规则标识变化后必须重新试写，不能采纳旧预演结果。

--- English ---

IMDb Tech Manager macOS v4.0.2

- About shows the actual September 2, 2026 release date instead of placeholder text.
- Opening Settings checks GitHub once for an official update and shows checking, up-to-date, update-available, or failure state in Software Update.
- Update checks are guarded against duplicate concurrent requests.
- Split-pane position, table column order and widths, Task Center expansion, and CLI height persist across launches; the native layer validates and restores window size and position.
- Movie and TV responsive toolbars, status-filter sizing, headers, and columns share consistent behavior without premature wrapping or overlap.
- Preview navigation aligns with the list header and marks the active tab with a short yellow line.
- Buttons, selectors, and structural headings do not select text; explanatory, status, and content text remains copyable.
- Background mode after app launch and launching the app after login are independent; old settings migrate safely.
- IMDb cache limits (default 2048 MB, range 64–65536 MB), usage state, cleanup, and LRU maintenance were added. Cleanup avoids symlinks, unknown files, and in-use entries.
- Raw IMDb cache entries are validated, and WAF or rate-limit failures remain explicit instead of being mistaken for missing Technical Specifications.
- Batch approvals wait and refresh by immutable task ID, while overlapping jobs fail explicitly.
- Local Rules generation and previews use the 4.0.0 rules identifier. Results must be previewed again after that identifier changes.
