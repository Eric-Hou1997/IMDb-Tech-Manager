# 安全与更新

请勿提交密钥、令牌、资料库样本或包含个人路径的诊断文件。

macOS 自动更新仅接受 GitHub 正式 Release 中经 Ed25519 签名的 ZIP 与同名 `.sig` 文件。私钥仅保存在维护者的本机安全目录，不得提交到仓库；发布前使用 `packaging/sign-update.sh` 生成签名。未通过签名验证的包不会替换当前应用。

未使用 Apple Developer ID 签名时，macOS 可能阻止更新后首次启动，或在系统策略变化时再次提示。请仅在确认下载来源为本项目 GitHub Release 后，在“隐私与安全性”中手动选择“仍要打开”。

安全问题请不要公开包含可利用细节的复现数据；先通过项目维护者的 GitHub 联系方式报告。
